server:
  port: 8080

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 12345
    pool:
      enabled: true
      initial-size: 5
      max-size: 20


logging:
  level:
    org:
      springframework:
        r2dbc: DEBUG
